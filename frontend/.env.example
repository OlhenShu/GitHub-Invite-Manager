# Backend API URL (leave empty to use relative URLs / Vite proxy)
# Default backend port is 8080. If that port is busy (common with WSL), run:
#   cd backend && set PORT=8081 && mvn spring-boot:run
# and set:
# VITE_API_URL=http://localhost:8081
VITE_API_URL=http://localhost:8080
